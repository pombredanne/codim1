CFLAGS += -g
CFLAGS += -Wall
CFLAGS += -O
CFLAGS += -std=c++11
CFLAGS += -I./src
CFLAGS += -lm
: foreach src/*.cpp |> g++ $(CFLAGS) -c %f -o %o |> %B.o
: main.cpp |> g++ $(CFLAGS) -c %f -o %o |> %B.o
: *.o |> g++ %f -o %o |> codim1fast
